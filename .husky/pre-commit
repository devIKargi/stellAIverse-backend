#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "🔍 Running pre-commit checks..."

# Lint and format staged files
# echo "📝 Linting and formatting staged files..."
# npx lint-staged

# Build check
echo "🔨 Checking build..."
npm run build

# Run risk-management tests if files were changed
# if git diff --cached --name-only | grep -q "src/risk-management"; then
#   echo "✅ Testing risk-management module..."
#   npm run test -- src/risk-management --coverage --forceExit
# fi

# Security audit
# echo "🔒 Running security audit..."
# npm audit --audit-level=moderate || true

echo "✅ Pre-commit checks passed!"
